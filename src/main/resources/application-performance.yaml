spring:
  jpa:
    properties:
      hibernate:
        show_sql: false

logging:
  level:
    org.hibernate.SQL: WARN
    org.hibernate.orm.jdbc.bind: WARN

cart:
  reservation:
    expiry:
      enabled: false

event:
  lock:
    strategy: ${EVENT_LOCK_STRATEGY:distributed}
